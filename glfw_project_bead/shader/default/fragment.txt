#version 330 core
    in vec2 bTex;

    uniform sampler2D textureSampler;
    out vec4 FragColor;
    void main()
    {
        vec4 pc = vec4(texture(textureSampler, bTex));
	float sum = pc.x + pc.y + pc.z; 
	if( sum < 0.15 ) { discard; }
	else { FragColor = pc; }
    }